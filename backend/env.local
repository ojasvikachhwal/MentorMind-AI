# Database Configuration
# For development, using SQLite (no installation required)
DATABASE_URL=sqlite:///./fastapi_app.db
# For PostgreSQL (uncomment and configure when PostgreSQL is installed)
# DATABASE_URL=postgresql://user:password@localhost/fastapi_db

# JWT Configuration
SECRET_KEY=your-super-secret-key-change-this-in-production
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Environment
ENVIRONMENT=development
